@FunctionalInterface
public interface IFuncionLambda {

	// método abstracto para realizar una operación aritmética con 2 números,
    // que se implementará a partir de una expresión Lambda

    public void calcular(int a, int b);
}
